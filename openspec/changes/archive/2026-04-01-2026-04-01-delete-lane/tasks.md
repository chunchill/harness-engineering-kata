# Implementation tasks: Delete lane

- [x] 1. OpenSpec: add spec delta; run `openspec validate 2026-04-01-delete-lane --strict`.
- [x] 2. Backend: add `DELETE /lanes/{id}` and `LaneService.delete(id)` with rules; add tests.
- [x] 3. Frontend: add delete-lane UI (column header button + confirm dialog); call API and refresh board.
- [x] 4. Validate: `npm run test`.

